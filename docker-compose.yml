services:
  voxscribe:
    build: .
    container_name: voxscribe
    ports:
      - "7860:7860"
    environment:
      OUTPUT_DIR: /outputs
      GRADIO_SHARE: "true"
    volumes:
      - ./outputs:/outputs
    restart: unless-stopped

  voxscribe-gpu:
    build: .
    container_name: voxscribe-gpu
    profiles: ["gpu"]
    ports:
      - "7860:7860"
    environment:
      OUTPUT_DIR: /outputs
      GRADIO_SHARE: "true"
    volumes:
      - ./outputs:/outputs
    gpus: all
    restart: unless-stopped
